import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['image.flaticon.com', 'upload.wikimedia.org', 'openclipart.org'],
  },
};

export default nextConfig;
